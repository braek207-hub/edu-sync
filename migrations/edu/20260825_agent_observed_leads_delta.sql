-- migrations/edu/20260825_agent_observed_leads_delta.sql
-- Наблюдаемая дельта лидов действия против базового ТЕМПА кампании
-- (agent_e1_watchdog.observed_leads_delta), заполняется при закрытии
-- наблюдения. Вторая половина пары «ожидание / факт»: ожидание солвера едет
-- в payload действия, факт появляется только по исходу — поэтому колонка, а
-- не дописывание в payload, который описывает намерение, а не результат.
--
-- NULL — «не измерено»: у действия не записан темп базы (спланировано до
-- появления этой пары) или окно наблюдения пустое. Отличать это от «эффекта
-- не было» обязана калибровка прогноза, иначе смещение модели считается по
-- выдуманным нулям.
ALTER TABLE edu_agent_actions
  ADD COLUMN IF NOT EXISTS observed_leads_delta DOUBLE PRECISION;
