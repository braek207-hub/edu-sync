-- migrations/edu/20260825_agent_learning_reset.sql
-- Класс действия по влиянию на обучение автостратегии Директа:
-- resets | safe | unknown (sync/agent/writer/learning.py). Хранится в
-- журнале, а не выводится на чтении: карта классов меняется вслед за
-- справкой Директа, история — нет. По этой колонке считается кулдаун
-- обучения (writer/db.py::last_learning_reset).
--
-- Старые строки остаются с NULL: чем они считались в момент применения,
-- неизвестно, и приписывать им класс задним числом значило бы выдать
-- сегодняшнюю карту за вчерашнее знание. Кулдаун такие строки не учитывает.
ALTER TABLE edu_agent_actions
  ADD COLUMN IF NOT EXISTS learning_impact TEXT;
