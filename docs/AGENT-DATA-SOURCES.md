# Источники данных агента-директолога — что API отдаёт на самом деле

Файл ведётся замерами, а не рассуждениями. Каждая строка — результат прогона probe
или сверки с кабинетом, с датой. Устаревшую строку не удаляем, а помечаем новым
замером ниже: опровергнутая гипотеза ценнее отсутствующей.

---

## Недобор трафика: чем он вообще измерим

**Замер:** `probe_traffic_headroom.py`, GitHub Actions run 32855656868, 2026-08-25,
conclusion success. Окно — 30 дней, кабинет EDU (4 проекта).

### Поля Reports API

| Поле | Ответ API | Вывод |
|---|---|---|
| `ImpressionShare` | FIELD_UNKNOWN | поля нет |
| `SearchImpressionShare` | FIELD_UNKNOWN | поля нет |
| `AuctionWinShare` | FIELD_UNKNOWN | поля нет |
| `AvgTrafficVolume` | OK | единственная живая мера недобора |

**Доли выкупа в Reports API не существует.** Колонка `direct_stats.w_auction_win_share`
и `edu_agent_facts.auction_win_share` пусты и наполнить их нечем: исторические значения
приходили выгрузкой интерфейса эпохи GAS, а не API. Объём трафика — другая величина
(какую долю трафика своей позиции берёт объявление), и называть её долей выкупа нельзя.

### Заполненность витрины

| Величина | Значение |
|---|---|
| Кампаний | 85 |
| Дней-кампаний | 2302 |
| Показов | 13 549 252 |
| Дней с ненулевым объёмом | 2302 из 2302 |
| Показов из дней с объёмом | все |
| **volume_coverage** | **1.0** |
| Средний объём трафика (взвешен показами) | 69.48 |
| Дней с непустой долей выкупа | 0 |

### Разрезы

| Разрез | Кампаний | volume_coverage | Средний объём |
|---|---|---|---|
| TEXT_CAMPAIGN | 70 | 1.0 | 65.32 |
| Нет в `edu_campaign_settings` | 15 | 1.0 | 84.36 |
| Только поиск | 67 | 1.0 | 52.59 |
| **Только сети** | **3** | **1.0** | **100.0** |
| Поиск + сети | 0 | — | — |

По проектам: vse 33, vuz 40, provuz 7, eduschool 5 — покрытие 1.0 везде.

### Что из этого следует для кода

1. **Недобор меряем объёмом трафика** (`AvgTrafficVolume`), не долей выкупа.
2. **Проверка покрытия ничего не отсекает** — пустых значений в витрине нет.
   `MIN_VOLUME_COVERAGE` в `sync/agent/headroom.py` остаётся страховкой на случай,
   если поле перестанет приходить, но защитой от сетей не является.
3. **Сетевые кампании из расчёта недобора исключаются по каналу показа.** Объём у них
   вырожден в константу 100.0: это не «весь трафик выкуплен», а «величина не
   измеряется». Оставленные в расчёте, они получили бы вердикт «выкуплен» и были бы
   вычеркнуты из кандидатов на рост. Канал читается из
   `edu_campaign_settings.settings #>> '{strategy,search|network,biddingStrategyType}'`,
   значение `SERVING_OFF` = канал выключен.
4. **15 кампаний (22 % показов) отсутствуют в витрине настроек** — канал показа
   неизвестен, вердикта они не получают. Это та же слепая зона, которую считает
   `sync/agent/coverage.py::blind_spend`.

### Опровергнутая гипотеза

До замера предполагалось: в сетях API отдаёт «--», `sync/direct.py:117` (`to_num_gas`)
превращает прочерк в ноль, и такой ноль прочитается как «недобор 100 %» — значит
сети зальёт карман разведки. **Неверно.** В сетях приходит 100.0 при полной
заполненности. Ошибка была бы зеркальной: не ложный недобор, а ложное «выкуплен».

---

## Полнота знаменателя расхода

**Статус: не сверено.** `blind_spend` (`sync/agent/coverage.py`) считает долю расхода,
у которого агент не видел настроек, но знаменатель берёт из `edu_agent_facts` — то есть
из того же Reports API, что и вся витрина. Расход, не попавший в витрину, невидим и для
этого счётчика, поэтому доля выходит оптимистичной.

Чтобы закрыть: один раз сверить сумму расхода витрины за календарный месяц с суммой
в интерфейсе Директа («Общая статистика» по всем кампаниям, привести к одной базе НДС)
и записать расхождение сюда. Совпало — знаменатель полон. Не совпало — в отчёте Э0
нужна вторая строка «расход вне витрины».

---

## Слепая зона: треть её была не Мастером кампаний, а сломанным синком

**Замеры 25.08.2026:** `probe_blind_spend_window` (run 32869110285 — состав зоны),
`probe_blind_campaigns_api` (та же серия — отдаёт ли API эти кампании),
`probe_settings_sync_failure` (run 32869597656 — где падает синк).

### Доля зависит от окна, и разница — это архив

| Окно | Слепая доля | Деньги | Кампаний |
|---|---|---|---|
| Окно лестницы (90 зрелых дней) | 30,3 % | 12,0 млн ₽ | 78 из 87 |
| **Окно решений (28 зрелых дней)** | **14,4 %** | **3,45 млн ₽** | **17 из 87** |

78 «слепых» кампаний зрелого окна за последние 28 дней не потратили ничего: это
закончившиеся кампании, а не невидимые. Одно число вместо двух заставляло бы не
доверять живым данным из-за архива, поэтому Э0 печатает обе доли, каждую со своим
окном (`blind_spend.decision_window` / `blind_spend.ladder_window`).

### 12 из 15 слепых кампаний API отдаёт

Проверка `campaigns.get` по явным Ids без фильтра состояний: 12 кампаний на
**1 007 402 ₽ за 28 дней** — обычные `TEXT_CAMPAIGN`, состояния ON/SUSPENDED,
статус ACCEPTED. То есть треть слепых денег агент мог видеть, и не видел.

### Причина: синк настроек падал на форме массивов и молчал

В прогоне `sync-edu-direct-settings` (run 32803336967) три кабинета из четырёх
кончались строкой `invalid literal for int() with base 10: 'Items'`; в витрину
уезжало **2 кампании из 129**, workflow при этом зелёный. Витрина жила на данных
прошлых удачных прогонов — кампании, созданные позже, в неё не попадали вовсе.

Две поломки в одном месте (`sync/edu_direct_settings.py`):

1. **Форма массивов.** API Директа отдаёт списки и голым массивом, и обёрткой
   `{"Items": [...]}`. Разбор групп брал форму на веру: `list(dict)` даёт ключи,
   то есть `["Items"]`, и следующий `int()` ронял кабинет целиком. Лечится
   функцией `_as_list`, которая в файле уже была и знает обе формы.
2. **Молчание.** Падение кабинета ловилось общим `except` и печаталось как WARN.
   Теперь остальные кабинеты досинхронизируются (частичная витрина лучше пустой),
   но прогон кончается красным с перечислением виновников.

**Проверка после фикса** (run 32871584331): `ИТОГО upsert 129`, все четыре кабинета,
ошибок нет. Снапшот стратегий — 136 строк.

**Вывод для оценки зоны.** Прежняя строка «слепая зона 15 % = Мастер кампаний и
прочее вне API» была неверна в части причины: как минимум треть этих денег —
обычные кампании, не доехавшие до витрины из-за дефекта. Остаток зоны нужно
мерить заново после нескольких успешных прогонов синка.

---

## Боевой такт расчёта после волны 1

**Прогон:** `agent-e0`, run 32873084562, 2026-08-25, conclusion success.
Вердикт GREEN, CRM зрелая по 2026-08-23 (лаг 2 дня), 12 612 строк фактов.

### Слепая доля — после починки синка настроек

| Окно | Доля | Слепые деньги | Кампаний |
|---|---|---|---|
| Окно решений (2026-07-27…08-23) | **6,18 %** | 1 290 829 ₽ из 20 899 111 ₽ | 25 из 111 |
| Окно лестницы (90 зрелых дней) | 30,28 % | 17 854 027 ₽ из 58 972 199 ₽ | 82 из 176 |

На окне решений было 14,4 % до починки синка — то есть **больше половины слепой
зоны решений оказалось дефектом кода, а не Мастером кампаний**. Окно лестницы не
сдвинулось: там правит архив, а не свежие кампании.

### Недобор трафика

Окно 2026-07-27…08-23, 77 кампаний с вердиктом:

| Величина | Значение |
|---|---|
| С недобором | **61** |
| Выкуплены | 2 |
| Расход кампаний с недобором | **19 570 683 ₽** |
| Строк в `edu_agent_computed_settings` | 142 |

Без вердикта — 14 кампаний, по четырём причинам: нет в витрине настроек (3),
между 70 и 90 (4), только сети (3), меньше 5000 показов (4).

### Режим спроса

| Направление | σ | Режим |
|---|---|---|
| `vpo` | −4,94 | **спад** |
| `spo` | +2,02 | **подъём** |
| `dist` | −0,42 | норма |
| `dpo` | +1,14 | норма |
| `school`, `it`, `med`, `mti`, `ntb`, `transfer` | — | **нет ряда** |

Ряд Wordstat кончается неделей 2026-08-10.

### Что усилить

25 кандидатов, запас **2 635 412 ₽**, и он неоднороден:

| Рычаг | Запас |
|---|---|
| Цена (`tcpa`) | 2 515 055 ₽ |
| Бюджет (`limit_binding`) | **120 357 ₽** |

Разница принципиальна: доливкой бюджета кабинет сегодня выберет 120 тыс ₽, всё
остальное упирается в цену, а не в лимит. 16 кампаний уткнулись в кап шага, одна
снята тормозом качества. Направление в подъёме одно — `spo`.

### Рост общей суммы: не разрешён, и правильно

Все четыре кабинета получили `capped_by: "lambda"` — λ 0.43…0.71, то есть
предельный рубль не окупается даже до цели, не то что с запасом. Рост 0 ₽ при
пустом `monthly_budget_cap_rub`. Это ожидаемое поведение, а не поломка: механизм
роста включается там, где предельная окупаемость выше цели.

### Тормоз качества и петля обучения

`lead_quality`: одна кампания помечена — средний ML-скор оплаты упал с 0.0155 до
0.0116 (−25 %) между смежными 28-дневными окнами, рост приостановлен до денежного
чекпоинта. Провалов покрытия скором нет.

`learning_loop`: `closed_actions: 0`, `track_record` и `forecast_bias` пусты.
Это НЕ дефект: закрытых вердиктов ещё нет, пара «прогноз / факт» появляется
только у действий, спланированных после введения ожиданий. Ноль обязан быть
виден числом, а не отсутствием секции — иначе «не научились» и «сломалось»
выглядят одинаково.

### Репетиция такта записи

**Прогон:** `agent-e1` (`--prod` без `--apply`), run 32877817788, 2026-08-25.

| Величина | Значение |
|---|---|
| Освобождено сокращениями | 465 692 ₽ |
| Назначено доливками | **0 ₽** |
| Ожидаемые лиды такта | **−57,9** |
| Заблокировано «сокращение без адресата» | **10** |
| Заперто кулдауном обучения | 16 |
| Отложено потолком прогона / риск-бюджетом | 89 / 43 |

**Гейт роста сработал ровно так, как задуман.** Такт состоял из одних
сокращений: доливки, которые могли бы принять эти деньги, отсеялись раньше —
потолком прогона и недельным риск-бюджетом. `gate.shrinking: true` → десять
сокращений сняты → `applied.shrinking: false` при пустом наборе. Механизм
роста при этом не сломан: `growth.candidates` того же дня непуст (25 кандидатов,
2,6 млн ₽ запаса), просто 95 % запаса лежит в рычаге ЦЕНЫ, а не бюджета —
доливкой лимитов кабинет сегодня выберет только 120 тыс ₽.

Это и есть смысл проверки из плана: «сжимающий такт — повод остановиться, а не
применять». Применять было нечего, и это правильный исход, а не пустой прогон.

### Найденное репетицией: два числа под одним именем

Э0 напечатал слепую долю 6,18 % (25 кампаний), Э1 в тот же день — 9,45 %
(3 кампании). Ядро счётчика общее, вход разный: такт записи подавал в него
`ctx["cost_28d_by_campaign"]` — средний дневной расход × 28. Для цены ошибки
это верно (важен темп), для доли денег — нет: кампания, отработавшая 5 дней из
28, входит туда темпом, растянутым на месяц, а нулевые выпадают вовсе.
Исправлено (`load_cost_by_campaign` — сумма за окно); следующая репетиция
должна дать долю того же порядка, что и Э0.

---

## Спрос Wordstat: чего ряд не покрывает

**Замер:** `edu_wordstat_demand`, 5338 строк, 2026-08-25. Наполняется
`sync/edu_demand.py::sync_edu_wordstat_demand` — 19 фраз × 2 региона × недели.
Читается агентом в `sync/agent/demand.py` (режим спроса по направлениям).

### Направления без ряда спроса

`EDU_DEMAND_PHRASES` покрывает четыре уровня образования; направления кампаний
задаёт `sync/classify.py::detect_direction`. Совпадают они не полностью.

| Направление | Фразы спроса | Чем лечится |
|---|---|---|
| `spo` | колледж, техникум, училище, ссуз, среднее профессиональное | — |
| `vpo` | вуз, университет, институт, высшее образование, бакалавриат, специалитет, магистратура, аспирантура | — |
| `dist` | дистанционное обучение, дистанционное образование, заочное обучение | — |
| `dpo` | переподготовка, повышение квалификации, профпереподготовка | спрос есть, **кампаний под него нет** |
| `school` | **нет ни одной** | добавить фразы в `sync/edu_demand.py` |
| `it` | **нет ни одной** | то же |
| `med` | **нет ни одной** | то же |
| `mti` | **нет ни одной** | то же |
| `ntb` | **нет ни одной** | то же |
| `transfer` | **нет ни одной** | то же |

Такие направления получают вердикт `нет ряда`, а **не** `мало данных`: первое
чинится семантикой, второе — временем, и слить их в один вердикт значит спрятать
дыру навсегда. Список печатается в отчёте Э0 отдельной строкой
(`demand_regime.no_series`). Самое дорогое имя в этом списке — `school`: запуск
направления 14.08.2026, то есть там, где ожидания от кампаний нужнее всего,
агент идёт вслепую относительно рынка.

### Регион берётся один — `ru`

В витрине есть оба региона (`ru` = 225, `msk` = 1), но агент читает только `ru`.
Причина не в данных, а в их отсутствии на другой стороне: **гео кампании нигде не
вычисляется.** `sync/classify.py::normalize_city_ip_segment` работает по городу
ЛИДА, а не по настройкам кампании, и сопоставить московский срез спроса с
московскими кампаниями сейчас нечем.

Всероссийский ряд включает Москву, поэтому режим по нему — консервативное
приближение, а не ошибка: у московских кампаний сезон может отличаться, и такого
расхождения агент не увидит. Складывать `ru` и `msk` нельзя — москвичи
посчитались бы дважды.

Закрыть: снимать гео из настроек кампании в `edu_agent_objects` и вести режим
отдельно по каждому срезу. Работа для Ф8.
